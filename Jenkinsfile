pipeline {
    agent any
    stages {
        stage("Build") {
            steps {
                echo "Checking Docker version..."
                sh "git --version"
            }
        }
    }
}
